version: '3.7'

networks:
  bridge:
    external:
      name: ${NETWORKS}

services:
  malaysia-spring:
    image: nginx:alpine
    container_name: 0verMalaysiaSpring
    restart: unless-stopped
    tty: true
    environment:
      - TZ=Asia/Taipei
    volumes:
      - ./app/dist/:/usr/share/nginx/html/malaysia_spring/
    networks:
      - bridge
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.malaysia-spring.entrypoints=websecure"
      - "traefik.http.routers.malaysia-spring.tls.certresolver=lets-encr"
      - "traefik.http.routers.malaysia-spring.rule=Host(``) && PathPrefix(`/malaysia_spring`)"
